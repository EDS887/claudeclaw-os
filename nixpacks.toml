[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build",
  "npm install -g @anthropic-ai/claude-code"
]

[start]
cmd = "node dist/index.js"
